const User = {
  name: "inder",
  email: "user@1.com",
  isActive: true
}

function createUser({name: string, isPaid: boolean}){

}

createUser({name: "inder", isPaid: false})

function createCourse():{name: string, isPaid: boolean}{
  return {name: "hello", isPaid: false}
}

export{}
