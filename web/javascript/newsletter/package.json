{
  "name": "newsletter",
  "version": "1.0.0",
  "description": "A newsletter app that sends newsletter when someone signs up",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "ui": "browser-sync start --proxy localhost:8000 --files=**/*  --ignore=node_modules --reload-delay 1000 --no-ui --no-inject-changes --no-open"
  },
  "author": "walruii",
  "license": "ISC",
  "dependencies": {
    "@mailchimp/mailchimp_marketing": "^3.0.80",
    "body-parser": "^1.20.2",
    "express": "^4.18.2"
  }
}
